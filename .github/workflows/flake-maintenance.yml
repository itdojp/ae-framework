name: Daily Flake Maintenance
on:
  schedule:
    - cron: '0 10 * * *' # Daily at 19:00 JST
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  maintenance:
    uses: ./.github/workflows/flake-stability.yml
    with:
      mode: maintenance
