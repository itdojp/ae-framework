name: Flake Detect
on:
  workflow_dispatch:
  schedule:
    - cron: '0 21 * * *' # JST 06:00
permissions:
  contents: read
  issues: write
  pull-requests: write
jobs:
  flake-detection:
    uses: ./.github/workflows/flake-stability.yml
    with:
      mode: detect
