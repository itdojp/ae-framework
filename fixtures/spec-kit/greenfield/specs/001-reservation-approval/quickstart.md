# Quickstart: Reservation Approval

1. Import the feature with `pnpm run spec-kit:import-feature`.
2. Validate generated Context Pack and ExecPlan v2 artifacts.
3. Attach the bridge report to PR review evidence.
