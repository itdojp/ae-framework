# ExecPlan: Web API / 05-pr

## Goal
- Prepare PR materials with traceability to spec/tests and executed checks.

## Checklist
- [ ] Link spec files (OpenAPI, BDD, property) in PR description
- [ ] List executed commands (lint/type/unit/integration/property/mutation quick)
- [ ] Attach artifacts if heavy-test comparison was run (`reports/heavy-test-trends.json`)
- [ ] Note cache usage (restore/store) and pending follow-ups

## Template snippet (paste into PR)
```
Changes
- ...

Tests
- pnpm run lint
- pnpm run test:fast
- pnpm run test:integration -- --runInBand
- pnpm run test:property -- --runInBand --maxWorkers=50%
- STRYKER_TIME_LIMIT=0 pnpm run pipelines:mutation:quick (if executed)

Spec links
- OpenAPI: spec/api/openapi.yml
- BDD: spec/bdd/web-api-sample.md
- Property: spec/properties/web-api-sample.md

Cache/trends
- restore/store/snapshot: (done/skip)
- trend report: reports/heavy-test-trends.json (if generated)
```
